# Default CIX Neo build configuration.
# Environment variables may override these values; command-line options win.

: "${CIX_NEXUS:=zj}"
: "${CIX_DISTRIBUTION:=trixie}"
: "${CIX_JOBS:=}"
: "${CIX_OUTPUT_ROOT:=}"

: "${CIX_BOARD:=evb}"
: "${CIX_BUILD_MODE:=release}"
: "${CIX_DOCKER_MODE:=none}"

: "${CIX_PACKAGE_VERSION:=}"
: "${CIX_KERNEL_VERSION:=}"
: "${CIX_KERNEL_SERIES:=}"
: "${CIX_KERNEL_TARBALL_URL:=}"
